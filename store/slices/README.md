# Store Slices

This directory will contain Zustand store slices for different feature areas:

- `blocks.ts` - Block management state
- `canvas.ts` - Canvas and layout state  
- `drag.ts` - Drag and drop state
- `ui.ts` - UI component state

Each slice should follow the pattern of separating state from actions for better organization.