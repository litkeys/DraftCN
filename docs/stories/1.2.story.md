# Story 1.2: Block Data Model and Template System

## Status

Approved

## Story

**As a** developer,  
**I want** to implement the template-based block architecture,  
**so that** blocks have reusable templates with customizable instances.

## Acceptance Criteria

1. BlockTemplate interface includes (typeId, name, category, thumbnail, dependencies, defaultProps, component, defaultWidth/Height, minimumWidth/Height)
2. Block instance interface includes (id, typeId, props, x, y, width, height, z, selected)
3. Template registry system created to store and manage available templates
4. Manual template registration process defined with clear structure
5. At least 3 sample templates registered (hero1, navbar1, footer2) from Shadcnblocks References
6. Registry supports template registration, retrieval by typeId, and instance creation
7. All position values stored in pixels as specified

## Tasks / Subtasks

1. [x] Create TypeScript interfaces for block system (AC: 1, 2)
   - [x] Create `types/block.ts` with Block interface including all required properties
   - [x] Create `types/template.ts` with BlockTemplate interface including all metadata
   - [x] Create `types/canvas.ts` with CanvasState interface for state management
   - [x] Export all interfaces from `types/index.ts` for centralized imports
2. [x] Implement block registry system (AC: 3, 6)
   - [x] ~~Create `lib/blocks/types.ts` with core block system types~~ (Removed - redundant, registry imports directly from types/)
   - [x] Create `lib/blocks/registry.ts` with BlockRegistry class implementation
   - [x] Implement `registerTemplate()` method for adding templates to registry
   - [x] Implement `getTemplate()` method for retrieving templates by typeId
   - [x] Implement `getTemplatesByCategory()` for category filtering
   - [x] Implement `generateBlockInstance()` for creating block instances from templates
3. [x] Adapt hero1 template from Shadcnblocks (AC: 5)
   - [x] Copy `Shadcnblocks References/hero1.tsx` to `templates/hero1.tsx`
   - [x] Export the Hero1 component properly for registration
   - [x] Add thumbnail placeholder at `public/thumbnails/hero1.svg` (used SVG instead of PNG for better placeholder)
4. [x] Adapt navbar1 template from Shadcnblocks (AC: 5)
   - [x] Copy `Shadcnblocks References/navbar1.tsx` to `templates/navbar1.tsx`
   - [x] Export the Navbar1 component properly for registration
   - [x] Add thumbnail placeholder at `public/thumbnails/navbar1.svg` (used SVG instead of PNG for better placeholder)
5. [x] Adapt footer2 template from Shadcnblocks (AC: 5)
   - [x] Copy `Shadcnblocks References/footer2.tsx` to `templates/footer2.tsx`
   - [x] Export the Footer2 component properly for registration
   - [x] Add thumbnail placeholder at `public/thumbnails/footer2.svg` (used SVG instead of PNG for better placeholder)
6. [x] Register all templates in the registry (AC: 4, 5, 6)
   - [x] Import hero1, navbar1, footer2 components in `lib/blocks/registry.ts`
   - [x] Create BlockTemplate objects for each template with all required metadata
   - [x] Set typeId as 'hero1', 'navbar1', 'footer2' following naming convention
   - [x] Define appropriate defaultProps using the default values from each component
   - [x] Set defaultWidth/Height dimensions based on template type (hero: 1200x600, navbar: 1200x120, footer: 1200x420) - all multiples of 60px for grid alignment
   - [x] Call registerTemplate() for each template
   - [x] Export the configured registry instance
   - [x] Document registration process with inline comments (AC: 4)
7. [ ] Validate pixel-based positioning (AC: 7)
   - [ ] Ensure all position properties (x, y, width, height) use number type for pixels
   - [ ] Verify no grid snapping logic in this story (that's Story 2.1)
   - [ ] Confirm interfaces support freeform positioning
8. [ ] Create unit tests for registry
   - [ ] Test template registration and retrieval in `tests/unit/lib/registry.test.ts`
   - [ ] Test block instance generation with unique IDs
   - [ ] Test category filtering functionality
   - [ ] Verify error handling for missing templates

## Dev Notes

### Previous Story Insights

- Project successfully initialized with Next.js 15.5.2, TypeScript strict mode, and App Router
- shadcn/ui components installed and configured in `components/ui/`
- Zustand 4.5.7 store initialized at `store/index.ts` with slices directory
- All required project directories created and ready for use

### Data Models

**[Source: architecture/data-models.md#Block]**

- Block interface requires: id (string), typeId (string), props (any), x/y/width/height (numbers in pixels), z (number for stacking), selected (boolean)
- Block instances reference templates via typeId
- Props must conform to template's specific interface

**[Source: architecture/data-models.md#BlockTemplate]**

- BlockTemplate requires: typeId, name, category, thumbnail, dependencies array, defaultProps, component (React.ComponentType), defaultWidth/Height, minimumWidth/Height
- Dependencies extracted from import statements
- Component is actual React component reference, not code string

**[Source: architecture/data-models.md#BlockRegistry]**

- Registry uses Map<string, BlockTemplate> for template storage
- Methods needed: registerTemplate(), getTemplate(), getTemplatesByCategory(), generateBlockInstance()
- Block instance generation creates unique IDs using timestamp + random string

### File Locations

**[Source: architecture/unified-project-structure.md]**

- Type definitions: `types/block.ts`, `types/template.ts`, `types/canvas.ts`
- Registry implementation: `lib/blocks/registry.ts`, `lib/blocks/types.ts`
- Template components: `templates/{name}.tsx`
- Thumbnails: `public/thumbnails/{typeId}.png`

### Component Specifications

**[Source: architecture/data-models.md#Methods for Block Template Development]**

- Templates are React functional components with typed props
- Each template has accompanying registration object with all metadata
- Manual registration process in central registry file
- Components reference global CSS classes from `app/globals.css`

**[Source: Shadcnblocks References]**

- Using existing shadcnblocks templates: hero1.tsx, navbar1.tsx, footer2.tsx
- These templates already have proper TypeScript interfaces and default props
- Templates use shadcn/ui components (Badge, Button, NavigationMenu, Sheet, etc.)
- Each template has comprehensive props for customization

**[Source: docs/adding-block-templates.md]**

- Components go in `templates/` directory as individual files
- Each template must be registered in `lib/blocks/registry.ts`
- Registration includes: typeId, name, category, thumbnail path, dependencies, defaultProps, component reference, dimensions
- TypeId naming convention: lowercase with hyphens (e.g., 'hero1', 'navbar1', 'footer2')
- Thumbnails go in `public/thumbnails/` with matching typeId names

### Technical Constraints

**[Source: architecture/tech-stack.md]**

- TypeScript 5.3+ with strict mode required
- React 19.0+ for component runtime
- Template management via JavaScript objects (manual registration)
- Global CSS for shared block styles
- No backend/database - all state in memory

### Testing Requirements

**[Source: architecture/testing-strategy.md#Frontend Tests]**

- Unit tests in `tests/unit/lib/` directory
- Test files named with `.test.ts` suffix
- Use Vitest 1.0+ when configured (future story)
- Focus on registry logic and template processing
- Component tests verify rendering and props

## Change Log

| Date       | Version | Description            | Author       |
| ---------- | ------- | ---------------------- | ------------ |
| 2025-09-08 | 1.0     | Initial story creation | Scrum Master |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4 (claude-sonnet-4-20250514)

### Debug Log References

None

### Completion Notes List

- Task 1 completed: Created all TypeScript interfaces for block system
- Block interface includes all required properties (id, typeId, props, x, y, width, height, z, selected)
- BlockTemplate interface includes all metadata properties with React.ComponentType for component
- CanvasState interface includes blocks array and canvas state management properties
- All interfaces exported from centralized types/index.ts
- Unit tests created to validate interface structure and types
- Used `any` type for props and component fields with ESLint disable comments - this is intentional as each template has its own specific prop interface (verified against shadcnblocks references)
- Task 2 completed: Implemented block registry system
- BlockRegistry class uses Map<string, BlockTemplate> for template storage
- Implemented all required methods: registerTemplate(), getTemplate(), getTemplatesByCategory(), generateBlockInstance()
- Added utility methods: getAllTemplates(), getCategories(), hasTemplate(), removeTemplate(), clear(), getTemplateCount()
- Block instance generation creates unique IDs using timestamp + random string format
- Comprehensive unit tests cover registration, retrieval, category filtering, instance generation, and error handling
- Singleton instance exported as blockRegistry for global access
- Removed redundant lib/blocks/types.ts file - registry now imports directly from types/ folder
- Task 3 completed: Adapted hero1 template from Shadcnblocks
- Hero1 component copied to templates/hero1.tsx with proper TypeScript interface (Hero1Props)
- Component includes comprehensive props structure: badge, heading, description, buttons (primary/secondary), image
- Default props provide complete example data for immediate use
- Uses shadcn/ui components: Badge, Button with proper imports
- Thumbnail placeholder created as SVG at public/thumbnails/hero1.svg with template info
- Component properly exported for registry registration
- Task 4 completed: Adapted navbar1 template from Shadcnblocks
- Navbar1 component copied to templates/navbar1.tsx with TypeScript interfaces (Navbar1Props, MenuItem)
- Complex navigation component with desktop and mobile menu implementations
- Props include logo, menu items with nested structure, auth buttons (login/signup)
- Uses shadcn/ui components: Accordion, Button, NavigationMenu, Sheet (installed via shadcn CLI)
- Mobile responsive with Sheet-based mobile menu and Accordion for nested items
- Thumbnail placeholder created as SVG at public/thumbnails/navbar1.svg
- Task 5 completed: Adapted footer2 template from Shadcnblocks
- Footer2 component copied to templates/footer2.tsx with TypeScript interfaces (Footer2Props, MenuItem)
- Kept original @/components/shadcnblocks/logo imports intact as requested
- Installed required logo components using npx shadcn@latest add https://shadcnblocks.com/r/logo
- Logo components (Logo, LogoImage, LogoText) installed and moved to components/shadcnblocks/logo.tsx
- Props structure: logo, tagline, menuItems array, copyright, bottomLinks
- Multi-column footer layout with responsive grid design using shadcnblocks Logo components
- Thumbnail placeholder created as SVG at public/thumbnails/footer2.svg
- Task 6 completed: Registered all templates in the registry
- All three templates (hero1, navbar1, footer2) imported into lib/blocks/registry.ts
- Complete BlockTemplate objects created with all required metadata: typeId, name, category, thumbnail, dependencies, defaultProps, component, dimensions
- TypeIds follow naming convention: 'hero1', 'navbar1', 'footer2' as specified
- DefaultProps extracted from template default values to ensure consistency
- Dimensions set as multiples of 60px for grid alignment: hero (1200x600), navbar (1200x120), footer (1200x420)
- All templates registered with blockRegistry.registerTemplate() calls
- Comprehensive inline documentation added explaining registration process
- Registry exports singleton instance for global access

### File List

- types/block.ts (created)
- types/template.ts (created)
- types/canvas.ts (created)
- types/index.ts (created)
- tests/unit/types/interfaces.test.ts (created)
- lib/blocks/registry.ts (created and configured with all template registrations)
- tests/unit/lib/registry.test.ts (created)
- templates/hero1.tsx (created)
- templates/navbar1.tsx (created)
- templates/footer2.tsx (created)
- public/thumbnails/hero1.svg (created)
- public/thumbnails/navbar1.svg (created)
- public/thumbnails/footer2.svg (created)
- components/ui/accordion.tsx (installed via shadcn CLI)
- components/ui/navigation-menu.tsx (installed via shadcn CLI)
- components/ui/sheet.tsx (installed via shadcn CLI)
- components/shadcnblocks/logo.tsx (installed from shadcnblocks registry)
- components/ui/context-menu.tsx (installed as dependency)

## QA Results
