# Story 1.5: Block Library Sidebar with Template Loading

## Status

Approved

## Story

**As a** user,  
**I want** to see available blocks in a sidebar,  
**so that** I can choose what to add to my design.

## Acceptance Criteria

1. Left sidebar displays at 20% viewport width
2. Shows all templates from registry with thumbnails
3. Templates organized by category from BlockTemplate data
4. Sidebar scrollable if content exceeds viewport height
5. Each template shows name and thumbnail image
6. Visual hover state indicates templates are draggable
7. Templates loaded from registry on component mount

## Tasks / Subtasks

- [x] Task 1: Create Block Library component (AC: 2, 3, 5, 7)

  - [x] Create `components/blocks/BlockLibrary.tsx` component file
  - [x] Import and use block registry singleton to get all available templates
  - [x] Group templates by category using the registry's `getCategories()` method
  - [x] Display each category as a section with header
  - [x] Use the existing registry methods: `getAllTemplates()`, `getTemplatesByCategory()`

- [x] Task 2: Create Template Card component (AC: 2, 5, 6)

  - [x] Create `components/blocks/TemplateCard.tsx` component file
  - [x] Display template thumbnail image (from template.thumbnail property)
  - [x] Show template name (from template.name property)
  - [x] Add hover state styling to indicate draggability (cursor: grab, subtle scale or shadow effect)
  - [x] Set up proper data attributes for drag identification (data-template-id={template.typeId})

- [ ] Task 3: Integrate BlockLibrary into existing Sidebar (AC: 2, 3, 7)

  - [ ] Update existing `components/layout/Sidebar.tsx` to import BlockLibrary component
  - [ ] Replace placeholder text with BlockLibrary component
  - [ ] Ensure templates are loaded and displayed when component mounts
  - [ ] Keep existing ScrollArea configuration (already handles overflow)

- [ ] Task 4: Add loading states and error handling

  - [ ] Add loading state while templates are being initialized
  - [ ] Handle case when no templates are available in registry
  - [ ] Add error boundary around template rendering
  - [ ] Display helpful messages if categories are empty

- [ ] Task 5: Write unit tests
  - [ ] Test BlockLibrary renders all templates from registry
  - [ ] Test templates are grouped by category correctly
  - [ ] Test TemplateCard displays thumbnail and name
  - [ ] Test hover states are applied correctly

## Dev Notes

### Previous Story Insights

From Story 1.4: Global CSS system successfully implemented with shadcnblocks styles. All CSS variables are properly loaded and available. No style conflicts detected. Templates can safely reference global CSS classes.

### Existing Implementation Status

**Already Completed from Previous Stories:**

- `components/layout/Sidebar.tsx` exists with 20% width (`w-1/5`), ScrollArea, and proper positioning
- `lib/blocks/registry.ts` fully implemented with singleton pattern and all required methods
- Three templates already registered: hero1, navbar1, footer2
- Header component exists and is integrated in layout
- Main layout structure properly set up with flex containers

### Component Specifications

Based on architecture documentation, the sidebar components should follow this structure:

**File Locations** [Source: architecture/unified-project-structure.md]:

- `components/layout/Sidebar.tsx` - Main sidebar container (✅ EXISTS - needs BlockLibrary integration)
- `components/blocks/BlockLibrary.tsx` - Template library display logic (TO CREATE)
- `components/blocks/TemplateCard.tsx` - Individual template card component (TO CREATE)
- `components/ui/scroll-area.tsx` - shadcn/ui ScrollArea component (✅ EXISTS)

**Component Architecture** [Source: architecture/frontend-architecture.md#Component Architecture]:

- Components must be functional components using React hooks
- Use React.memo for performance optimization where drag operations are involved
- Follow the component template pattern shown in architecture docs

**State Management** [Source: architecture/frontend-architecture.md#State Management Architecture]:

- Templates state should be managed in Zustand store
- Store structure includes:
  - `templates: Map<string, BlockTemplate>`
  - `categories: string[]`
- Templates should be loaded into store on app initialization

**Block Registry Integration** [Source: architecture/components.md#Block Library Sidebar]:
The Block Library Sidebar component has these key interfaces:

- `getTemplatesByCategory(category: string)` - Filter templates by category (✅ EXISTS in registry)
- `onDragStart(typeId: string)` - Initiate template drag (will be implemented in Story 1.6)
- `renderThumbnail(template: BlockTemplate)` - Display template preview

**Existing Registry Implementation** [Source: lib/blocks/registry.ts]:
The registry singleton is already available with these methods:

- `blockRegistry.getAllTemplates()` - Returns all registered templates
- `blockRegistry.getCategories()` - Returns unique category list
- `blockRegistry.getTemplatesByCategory(category)` - Returns templates for a category
- Three templates already registered: hero1, navbar1, footer2

**BlockTemplate Interface** [Source: architecture/data-models.md#BlockTemplate]:

```typescript
interface BlockTemplate {
  typeId: string // Unique identifier
  name: string // Display name
  category: string // For grouping
  thumbnail: string // Base64 or URL
  component: React.ComponentType<any>
  // ... other properties
}
```

**Template Service Pattern** [Source: architecture/frontend-architecture.md#Frontend Services Layer]:
Templates are loaded through a service pattern:

```typescript
class TemplateService {
  private templates: Map<string, BlockTemplate> = new Map()

  async loadTemplates(): Promise<void> {
    const templates = await import('@/templates')
    templates.forEach((template) => {
      this.templates.set(template.typeId, template)
    })
  }

  getTemplatesByCategory(category: string): BlockTemplate[] {
    return Array.from(this.templates.values()).filter(
      (t) => t.category === category
    )
  }
}
```

**Tech Stack Requirements** [Source: architecture/tech-stack.md]:

- React 19.0+ for UI components
- TypeScript 5.3+ with strict mode
- Tailwind CSS 3.4+ for styling
- shadcn/ui components (ScrollArea)
- Zustand 4.5+ for state management

**Coding Standards** [Source: architecture/coding-standards.md]:

- Components: PascalCase (e.g., `BlockLibrary.tsx`)
- Hooks: camelCase with 'use' prefix
- CSS classes: kebab-case
- All code must be TypeScript with strict mode

### Testing Requirements

**Testing Standards** [Source: architecture/testing-strategy.md#Frontend Component Test]:

- Test files location: `tests/components/` directory
- Use Vitest 1.0+ for unit testing
- Use @testing-library/react for component testing
- Test file naming: `BlockLibrary.test.tsx`, `Sidebar.test.tsx`

Required test coverage:

- Sidebar width calculation (20% viewport)
- Template loading from registry
- Category grouping logic
- Scroll behavior when content overflows
- Hover state changes
- Error states and loading states

## Change Log

| Date       | Version | Description            | Author             |
| ---------- | ------- | ---------------------- | ------------------ |
| 2025-09-10 | 1.0     | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-1-20250805

### Debug Log References

- Task 1 implementation started at 2025-09-10
- BlockLibrary component created successfully
- Used existing registry singleton pattern from lib/blocks/registry.ts
- Implemented category grouping and template display
- Task 2 implementation completed
- TemplateCard component created with hover states and data attributes

### Completion Notes List

- Task 1 completed: BlockLibrary component created with template loading and category grouping
- Task 2 completed: TemplateCard component created with thumbnail display and hover states
- Components follow TypeScript strict mode requirements
- Includes loading states and empty state handling
- Properly integrates with existing block registry singleton
- Fixed import path for BlockTemplate from @/types/template
- TemplateCard shows thumbnails, names, and hover effects (cursor-grab, scale, shadow)
- Data attributes set for drag identification (data-template-id)

### File List

**Created:**
- components/blocks/BlockLibrary.tsx - Main block library component with template loading and category grouping
- components/blocks/TemplateCard.tsx - Template card component with thumbnail, name, and hover states

**Modified:**
- components/blocks/BlockLibrary.tsx - Updated to import and use TemplateCard component

## QA Results

_To be populated by QA agent_
