# Story 1.9: Fixed-Width Canvas with Container and Auto-Expansion

## Status

Done

## Story

**As a** user,
**I want** a fixed-width canvas that is clearly distinguished from its container and expands automatically as I add content,
**so that** I have a predictable workspace with unlimited vertical space.

## Acceptance Criteria

1. Canvas container fills entire available space within parent flex container with distinct background color
2. Canvas itself is exactly 1200px wide and horizontally centered within container
3. Canvas has minimum height of 1200px
4. Canvas height dynamically calculated based on lowest block position plus 1200px buffer
5. Canvas visually distinguished from container (different background color, subtle border/shadow)
6. Canvas background is white, container background is light gray
7. Blocks can only be dropped within the 1200px canvas boundaries (dead zones are container areas outside canvas)
8. Canvas re-calculates height when blocks are added/moved/removed
9. Scrollable vertically when canvas content exceeds viewport height

## Tasks / Subtasks

- [x] Task 1: Refactor Canvas component structure to separate container and canvas (AC: 1, 2, 5, 6)

  - [x] Wrap existing canvas div with container div
  - [x] Apply container styling: full width within parent flex container (`w-full h-full`), light gray background (`bg-gray-100`)
  - [x] Update inner canvas: exactly 1200px width, white background (`bg-white`)
  - [x] Center canvas horizontally using `mx-auto`
  - [x] Add subtle visual distinction: `shadow-md border border-gray-200`
  - [x] Update refs and event handlers to maintain proper hierarchy

- [x] Task 2: Additional improvements and fixes (Beyond original scope)

  - [x] Fixed canvas background color change during drag (removed drag-over class)
  - [x] Fixed block rendering containment issues
  - [x] Changed from border to outline for canvas border (no space consumption)
  - [x] Changed block selection from border to outline (no layout impact)
  - [x] Removed position constraints during drag for smooth movement
  - [x] Added position correction on mouse up (snap to valid boundaries)
  - [x] Added position correction on mouse leave (consistent behavior)
  - [x] Fixed drag lag by removing transition-all during position updates
  - [x] Added container click handler for block deselection
  - [x] Auto-select newly dropped blocks from library

- [x] Task 3: Implement dynamic height calculation logic (AC: 3, 4, 8)

  - [x] Create `calculateCanvasHeight` function in Canvas component
  - [x] Calculate lowest block position: `Math.max(...blocks.map(b => b.y + b.height))`
  - [x] Return `Math.max(1200, lowestPosition + 1200)` for minimum height with buffer
  - [x] Use calculated height in canvas inline style
  - [x] Trigger recalculation when blocks array changes (add/remove/move)

- [x] Task 4: Write comprehensive unit tests
  - [x] Test container width is full width of parent (w-full)
  - [x] Test canvas width is exactly 1200px
  - [x] Test canvas horizontal centering
  - [x] Test minimum height of 1200px
  - [x] Test dynamic height calculation with blocks
  - [x] Add tests to existing Canvas.test.tsx file

## Dev Notes

### Previous Story Context

Story 1.8 successfully implemented block movement on canvas with:

- `handleBlockMouseDown` for drag initiation with automatic selection
- `handleMouseMove` for real-time position updates during drag
- Proper coordinate system using canvas-relative positions
- Mouse offset calculation for accurate dragging
- Routing logic in `handleMouseUp` between canvas drag and library drop
- Hover effect suppression during drag operations
- **Key Learning**: Coordinate system consistency is critical - all calculations must use canvas-relative coordinates

### Current Implementation Analysis

**Current Canvas Structure** [Source: components/canvas/Canvas.tsx]:

```typescript
// Current: Single div serving as both container and canvas
<div
  ref={canvasRef}
  className="relative w-full h-full bg-slate-50 overflow-auto"
  style={{ minHeight: '100vh' }}
  // ... event handlers ...
>
  {/* Blocks rendered here */}
</div>
```

**Required Refactor to Nested Structure**:

```typescript
// New: Container wrapping the actual canvas
<div className="container-styles">
  <div ref={canvasRef} className="canvas-styles">
    {/* Blocks rendered here */}
  </div>
</div>
```

### Existing Store Methods to Use

**BlocksSlice Methods** [Source: store/slices/blocks.ts]:

```typescript
interface BlocksState {
  blocks: Block[]
  selectedBlockIds: string[]
}

// Available methods:
addBlock(block: Block): void
updateBlock(id: string, updates: Partial<Block>): void
removeBlock(id: string): void
getAllBlocks(): Block[]  // via blocksSelectors
```

**Block Interface** [Source: types/block.ts]:

```typescript
interface Block {
  id: string
  typeId: string
  x: number // Canvas-relative position
  y: number // Canvas-relative position
  width: number
  height: number
  z: number
  selected?: boolean
  props: Record<string, any>
}
```

### Canvas Component Event Handlers

**Existing Handlers That Need Updates** [Source: components/canvas/Canvas.tsx]:

1. **handleBlockMouseDown** (lines 40-80): Uses `canvasRef.current.getBoundingClientRect()` - must continue using canvas element, not container

2. **handleMouseMove** (lines 208-227): Uses `canvasRef.current.getBoundingClientRect()` for position calculations

3. **handleLibraryDrop** (lines 138-175): Creates blocks at drop position - needs boundary checking

4. **Coordinate Conversion Pattern**:

```typescript
const rect = canvasRef.current.getBoundingClientRect()
const canvasX = e.clientX - rect.left
const canvasY = e.clientY - rect.top
```

### Dynamic Height Calculation Implementation

**Calculation Logic**:

```typescript
const calculateCanvasHeight = useCallback(() => {
  if (blocks.length === 0) {
    return 1200 // Minimum height when empty
  }

  // Find the lowest point of all blocks
  const lowestPoint = Math.max(...blocks.map((block) => block.y + block.height))

  // Return minimum of 1200px or lowest point + 1200px buffer
  return Math.max(1200, lowestPoint + 1200)
}, [blocks])
```

### Boundary Checking for Drops

**Drop Validation Logic**:

```typescript
const isValidDropPosition = (x: number, y: number, width: number): boolean => {
  // Check if drop is within canvas boundaries
  if (x < 0 || x + width > 1200) return false
  if (y < 0) return false
  return true
}
```

### Container and Canvas Styling

**Container Styles**:

- Width: `w-full` (full width within parent flex container)
- Height: `h-full` (full height within parent flex container)
- Background: `bg-gray-100` (light gray)
- Overflow: `overflow-y-auto overflow-x-hidden`
- Padding: `p-8` (spacing around canvas)

**Canvas Styles**:

- Width: `w-[1200px]` (exactly 1200px)
- Background: `bg-white` (white)
- Border: `border border-gray-200`
- Shadow: `shadow-md`
- Position: `relative` (for absolute positioned blocks)
- Margin: `mx-auto` (horizontal centering)
- No overflow styles (handled by container)

### File Locations

**Based on Project Structure** [Source: architecture/unified-project-structure.md]:

- Canvas updates: `components/canvas/Canvas.tsx` (existing file - refactor structure)
- No new files needed - refactoring existing Canvas component
- Tests: `tests/unit/components/canvas/Canvas.test.tsx` (update existing tests for new structure)

### Testing Requirements

**Test Standards** [Source: architecture/testing-strategy.md]:

- Location: `tests/unit/components/canvas/Canvas.test.tsx`
- Framework: Vitest with React Testing Library
- Use existing mock patterns for `useAppStore` and `dragManager`

**Required Test Updates**:

1. **DOM Structure Tests**: Update selectors to account for nested structure

```typescript
// Old: canvas is the root element
const canvas = getByTestId('canvas')

// New: canvas is nested within container
const container = getByTestId('canvas-container')
const canvas = within(container).getByTestId('canvas')
```

2. **Dimension Tests**: New tests for container and canvas dimensions

```typescript
it('should set container to full width within parent', () => {
  const { getByTestId } = render(<Canvas />)
  const container = getByTestId('canvas-container')
  expect(container).toHaveClass('w-full h-full')
})

it('should set canvas to exactly 1200px width', () => {
  const { getByTestId } = render(<Canvas />)
  const canvas = getByTestId('canvas')
  expect(canvas).toHaveStyle({ width: '1200px' })
})
```

3. **Height Calculation Tests**: Test dynamic height with blocks

```typescript
it('should calculate height based on lowest block position', () => {
  // Add blocks at various positions
  // Verify canvas height = lowest block bottom + 1200px
})
```

4. **Boundary Tests**: Test drop validation

```typescript
it('should prevent drops outside canvas boundaries', () => {
  // Attempt drop at x = -50
  // Verify block not created
})
```

### Technical Standards

**Technology Stack** [Source: architecture/tech-stack.md]:

- React 19.0+
- TypeScript 5.3+ with strict mode
- Tailwind CSS 3.4+ for styling
- Vitest 1.0+ for testing

**Coding Standards** [Source: architecture/coding-standards.md]:

- Use `useCallback` for event handlers
- Maintain immutability
- TypeScript types for all props and events
- Follow existing `useAppStore` patterns

### Important Implementation Notes

1. **Ref Management**: Keep `canvasRef` pointing to the actual canvas element (not container) to maintain correct coordinate calculations in all drag handlers

2. **Event Bubbling**: Ensure click and mouse events properly bubble/stop between container and canvas elements

3. **Performance**: Use `useMemo` for height calculation if needed to prevent unnecessary recalculations

4. **Backwards Compatibility**: All existing block position logic remains valid as positions are canvas-relative, not container-relative

5. **Test Impact**: Many existing tests will need updates to account for the nested DOM structure

## Change Log

| Date       | Version | Description                                         | Author             |
| ---------- | ------- | --------------------------------------------------- | ------------------ |
| 2025-01-17 | 1.0     | Initial story creation                              | Bob (Scrum Master) |
| 2025-01-17 | 1.1     | Task 1 completed - Canvas/container structure       | Dev Agent          |
| 2025-01-17 | 1.2     | Task 2 added & completed - Bug fixes & improvements | Dev Agent          |
| 2025-01-18 | 1.3     | Task 3 completed - Dynamic height calculation       | Dev Agent          |
| 2025-01-18 | 1.4     | Task 4 completed - Comprehensive unit tests         | Dev Agent          |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.1

### Debug Log References

**Initial Implementation (Task 1):**

- Canvas component refactored with nested container/canvas structure
- Container styles applied: w-full h-full bg-gray-100 overflow-y-auto overflow-x-hidden p-8
- Canvas styles initially: w-[1200px] bg-white shadow-md border border-gray-200 mx-auto
- Canvas minimum height set to 1200px
- Tests updated for new DOM structure (container and nested canvas)

**Bug Fixes & Improvements (Task 2):**

- Fixed drag-over background color change by removing the class and style completely
- Fixed border clipping by switching from border to outline (outline-1 outline-gray-200)
- Fixed block selection using outline instead of border (outline-2 outline-blue-500)
- Fixed drag lag by removing transition-all class from block positioning
- Fixed position constraints - removed during drag, added on mouse up and mouse leave
- Added handleContainerClick for deselection anywhere in workspace
- Added auto-selection of newly dropped blocks from library
- Improved drag smoothness by deferring all position corrections to drag end

**Task 3 - Dynamic Height Calculation (Completed):**

- Implemented calculateCanvasHeight function using useMemo for optimal performance
- Calculates lowest block position by finding max of all block bottoms (y + height)
- Maintains minimum height of 1200px when canvas is empty
- Adds 1200px buffer below the lowest block for comfortable workspace
- Automatically recalculates when blocks array changes (add/move/remove)
- Height applied via minHeight style property on canvas element
- Fixed linting errors (const instead of let for immutable values)

**Task 4 - Comprehensive Unit Tests (Completed):**

- All required tests verified to be already implemented
- Container width test validates w-full (full width of parent)
- Canvas width test confirms exactly 1200px width
- Canvas centering test verifies mx-auto class
- Minimum height test validates 1200px baseline
- Dynamic height calculation suite includes 6 comprehensive tests
- Additional scrolling behavior tests (7 tests) added for robustness
- Boundary validation tests (4 tests) added for drop constraints

**All Canvas tests passing (82 tests total)**

### Completion Notes List

**Task 1 - Canvas Structure (Completed):**

- Canvas now has proper container/canvas separation
- Container provides scrolling and gray background
- Canvas is fixed at 1200px width with white background
- Visual distinction achieved with shadow and outline
- All existing event handlers maintained and working

**Task 2 - Additional Improvements (Completed):**

- Drag experience significantly improved (no lag, smooth movement)
- Selection indicators properly render without affecting layout
- Canvas boundaries enforced only when appropriate (drop/release/leave)
- Block deselection works from any non-block area (canvas or container)
- New blocks are auto-selected for immediate manipulation
- Position correction ensures blocks stay within valid boundaries

**Task 3 - Dynamic Height Calculation (Completed):**

- Canvas height dynamically adjusts based on block positions
- Minimum height of 1200px maintained for empty canvas
- 1200px buffer added below lowest block for workspace comfort
- Efficient recalculation using useMemo hook with blocks dependency
- Comprehensive test coverage for all height calculation scenarios

**Task 4 - Comprehensive Unit Tests (Completed):**

- Container styling tests verify w-full, h-full, bg-gray-100, overflow settings
- Canvas dimensions test confirms exactly 1200px width
- Canvas positioning test validates mx-auto horizontal centering
- Height tests verify 1200px minimum and dynamic calculation with blocks
- Test suite includes 82 total tests covering all Canvas functionality
- All tests passing and providing complete coverage

**Technical Decisions Made:**

- Used outline instead of border to avoid box model space consumption
- Removed CSS transitions during drag for immediate responsiveness
- Deferred position correction to drag end for smooth UX
- Applied same correction logic for both mouse up and mouse leave events
- Used useMemo for height calculation to prevent unnecessary recalculations

**Tests updated and passing for all changes**

### Change Summary (Task 2 - Additional Improvements)

**Issues Fixed:**

1. **Drag-over background darkening** - Removed drag-over class entirely
2. **Selection border clipping** - Switched to outline for space-free rendering
3. **Drag lag/stuttering** - Removed transition-all during drag operations
4. **Position constraints during drag** - Deferred to drag end for smooth movement
5. **Limited deselection area** - Added container click handler

**Features Added:**

1. **Position correction on mouse leave** - Consistent with mouse up behavior
2. **Auto-select on drop** - New blocks from library are automatically selected
3. **Container-wide deselection** - Click anywhere outside blocks to deselect

**Technical Improvements:**

1. Canvas uses `outline` instead of `border` (true 1200px internal width)
2. Blocks use `outline` for selection (no layout shift)
3. Event handlers optimized for smooth drag experience
4. Position correction logic consolidated and consistent

### File List

- Modified: components/canvas/Canvas.tsx (extensive changes - structure, event handlers, styling, dynamic height calculation)
- Modified: tests/unit/components/canvas/Canvas.test.tsx (updated for new structure, outline classes, and added dynamic height tests)

## QA Results

### Review Date: 2025-01-18

### Reviewed By: Quinn (Test Architect)

### Quality Assessment

**Story 1.9: Fixed-Width Canvas with Container and Auto-Expansion**

#### Acceptance Criteria Verification

✅ **All 9 acceptance criteria fully met:**

1. ✅ Canvas container fills entire available space with distinct background (bg-gray-100)
2. ✅ Canvas exactly 1200px wide and horizontally centered (w-[1200px] mx-auto)
3. ✅ Canvas has minimum height of 1200px (verified in code and tests)
4. ✅ Dynamic height calculation implemented (lowestPoint + 1200px buffer)
5. ✅ Visual distinction achieved (white canvas, gray container, shadow-md, outline)
6. ✅ Color scheme implemented (canvas: bg-white, container: bg-gray-100)
7. ✅ Drop boundaries enforced (position correction on mouse up/leave)
8. ✅ Height recalculation on block changes (useMemo with blocks dependency)
9. ✅ Vertical scrolling working (overflow-y-auto on container)

#### Technical Quality

**Architecture & Implementation:**

- Clean separation of container and canvas concerns
- Proper use of React hooks (useMemo for performance)
- Position correction logic consolidated and consistent
- Event handling properly structured with clear boundaries

**Code Quality:**

- Follows project coding standards
- TypeScript strict mode compliance
- Tailwind CSS conventions maintained
- No console errors or warnings

**Testing:**

- **82 tests passing** - comprehensive coverage
- Container/canvas dimension tests
- Dynamic height calculation tests (6 scenarios)
- Scrolling behavior tests (7 scenarios)
- Boundary validation tests (4 scenarios)
- All edge cases covered

#### Additional Improvements Beyond Scope

The development team proactively addressed several UX issues:

- Fixed drag-over background color changes
- Switched to outline for space-free rendering
- Removed drag lag by optimizing transitions
- Added container-wide deselection
- Auto-selection of newly dropped blocks
- Position correction on both mouse up and leave

#### Risk Assessment

**Low Risk** - Story ready for production:

- All requirements fully implemented
- Comprehensive test coverage (82 tests)
- No security concerns
- No performance issues
- Clean, maintainable code

### Gate Status

Gate: PASS → docs/qa/gates/1.9-fixed-width-canvas.yml

## Development Summary

**Story 1.9 Complete - Ready for Review**

All 4 tasks successfully implemented:

- ✅ Canvas/container structure refactored
- ✅ Bug fixes and UX improvements applied
- ✅ Dynamic height calculation implemented
- ✅ Comprehensive test suite (82 tests) passing

The canvas now provides a fixed 1200px width workspace with automatic vertical expansion, clear visual distinction from its container, and smooth drag-and-drop interactions. All acceptance criteria have been met.
