# Story 1.13: Export to React Project

## Status

Approved

## Story

**As a** user,
**I want** to export my design as a minimal React project,
**so that** I can have a working codebase that preserves my exact layout for further development.

## Acceptance Criteria

1. Export Infrastructure: Export button in top navigation includes "Export to React" option that triggers React project generation with loading state and success notification
2. Template Source Resolution: Template source code is retrieved from dedicated mapping system (`lib/blocks/template-sources.ts`) that stores pre-processed template code with proper import paths for export compatibility
3. Static Asset Resolution: Application assets (globals.css) are retrieved from dedicated source files (`lib/project/globals.css.source.ts`) to avoid embedding large static content in export logic
4. Component Generation: For each unique template used in canvas blocks:
   - Template source code is retrieved from the mapping system using block typeId
   - Component file is generated with proper TypeScript interfaces and exports
   - Import paths are pre-processed for export structure (`@/components/ui/*` → `./ui/*`)
   - shadcn/ui dependencies (Badge, Button, Card, etc.) are included as separate component files
   - shadcnblocks dependencies (Logo components) are included with their utility dependencies
5. Project Structure Assembly: Export generates a complete React project ZIP with:
   - `/src/components/` containing all template components and their dependencies
   - `/src/App.tsx` that imports and renders blocks with absolute positioning
   - `/src/index.tsx` React 18+ entry point with proper CSS import order
   - `/src/globals.css` generated from source file with all theming and fonts
   - Root `index.html` with React mount point and responsive viewport
   - `package.json` with React, TypeScript, Tailwind CSS, and Vite dependencies
   - `README.md` with setup instructions and architecture explanation
6. Block-to-Component Mapping: Canvas blocks are transformed to React components where:
   - Each block maps to its template component using typeId lookup
   - Block props are passed directly to component instances
   - Y-positioning is preserved for absolute layout (`top: ${block.y}px`)
   - X-positioning is ignored in favor of full-width responsive design
   - Z-index determines rendering order for proper layering
7. Export File Structure: Generated project uses clean architecture:
   - Standard Tailwind CSS (no custom config required)
   - CSS custom properties in globals.css for theming consistency
   - TypeScript throughout for better developer experience
   - Vite for fast development and optimized production builds
   - Component dependencies resolved and included automatically
8. User Experience: Export process provides:
   - Descriptive ZIP filename: `draftcn-react-export-[YYYY-MM-DD-HHmmss].zip`
   - Automatic download upon completion
   - Loading indicator during generation
   - Success notification: "React project exported successfully"
   - Clear README with `npm install && npm run dev` quick start
9. Error Handling: Robust error management for:
   - Missing template source mappings with fallback placeholder components
   - Component generation failures with specific error messages
   - ZIP creation errors with user-friendly notifications
   - Dependency resolution failures with graceful degradation

## Tasks / Subtasks

### Phase 1: Template Source Mapping System

- [x] Task 1: Create template source mapping system (AC: 2)

  - [x] Create `lib/blocks/template-sources.ts` with TypeScript interface for template source mapping
  - [x] Define `TemplateSourceMap` interface with typeId keys and source code values
  - [x] Extract source code from existing templates in `templates/` directory
  - [x] Pre-process import paths for export compatibility (`@/components/ui/*` → `./ui/*`)
  - [x] Store each template's full TypeScript source code with proper exports
  - [x] Add tests in `tests/unit/lib/blocks/template-sources.test.ts`

- [ ] Task 2: Create static asset source files (AC: 3)
  - [ ] Create `lib/project/globals.css.source.ts` to store globals.css content
  - [ ] Export the CSS as a string constant from existing `app/globals.css`
  - [ ] Include all Tailwind directives, custom properties, and font imports
  - [ ] Create `lib/project/static-sources.ts` for other static assets (package.json template, etc.)
  - [ ] Add tests in `tests/unit/lib/project/static-sources.test.ts`

### Phase 2: React Code Generation

- [ ] Task 3: Create React export utilities (AC: 4, 5, 6)

  - [ ] Create `lib/project/react-export.ts` with main export logic
  - [ ] Implement `generateReactProject()` function that accepts blocks from store
  - [ ] Create `extractUniqueTemplates()` to identify all unique typeIds used
  - [ ] Implement `generateComponentFile()` to create individual component files
  - [ ] Create `resolveDependencies()` to collect all required npm packages
  - [ ] Add ZIP file generation using JSZip library (`npm install jszip @types/jszip`)
  - [ ] Add tests in `tests/unit/lib/project/react-export.test.ts`

- [ ] Task 4: Implement component dependency resolution (AC: 4)

  - [ ] Create `lib/project/dependency-resolver.ts`
  - [ ] Parse template imports to identify shadcn/ui components used
  - [ ] Generate shadcn/ui component files based on actual usage
  - [ ] Include utility dependencies (cn utility, types, etc.)
  - [ ] Handle lucide-react icon dependencies
  - [ ] Add tests in `tests/unit/lib/project/dependency-resolver.test.ts`

- [ ] Task 5: Generate main App component (AC: 5, 6)
  - [ ] Create `generateAppComponent()` in `lib/project/react-export.ts`
  - [ ] Import all used template components dynamically
  - [ ] Map blocks to component instances with absolute positioning
  - [ ] Preserve Y-positioning using `style={{ position: 'absolute', top: block.y }}`
  - [ ] Apply z-index for proper layering
  - [ ] Pass block.props to each component instance
  - [ ] Add container with relative positioning for absolute children

### Phase 3: Project Structure Assembly

- [ ] Task 6: Create project file structure generator (AC: 5, 7)

  - [ ] Implement `generateProjectFiles()` in `lib/project/react-export.ts`
  - [ ] Generate `/src/components/` directory structure with template components
  - [ ] Create `/src/components/ui/` with required shadcn components
  - [ ] Generate `/src/App.tsx` using the App component generator
  - [ ] Create `/src/index.tsx` as React 18+ entry point
  - [ ] Generate `/src/globals.css` from the source file
  - [ ] Create root `index.html` with React mount point
  - [ ] Generate `package.json` with all collected dependencies
  - [ ] Create `vite.config.ts` for Vite build configuration
  - [ ] Generate `tsconfig.json` for TypeScript configuration
  - [ ] Create `tailwind.config.js` with standard configuration
  - [ ] Generate `README.md` with setup and usage instructions

- [ ] Task 7: Implement ZIP file creation and download (AC: 8)
  - [ ] Use JSZip to create archive with all project files
  - [ ] Implement `downloadReactProject()` similar to existing `downloadJSON()`
  - [ ] Generate filename with format: `draftcn-react-export-[YYYY-MM-DD-HHmmss].zip`
  - [ ] Trigger browser download using Blob and anchor element
  - [ ] Add loading state management during generation

### Phase 4: UI Integration

- [ ] Task 8: Add "Export to React" option to ExportButton (AC: 1, 8)
  - [ ] Update `components/layout/ExportButton.tsx`
  - [ ] Add new `DropdownMenuItem` for "Export to React"
  - [ ] Implement `handleExportReact()` click handler
  - [ ] Add loading state using existing button disabled state pattern
  - [ ] Show toast notification using Sonner: `toast.success("React project exported successfully")`
  - [ ] Update tests in `tests/unit/components/layout/ExportButton.test.tsx`

### Phase 5: Error Handling and Polish

- [ ] Task 9: Implement comprehensive error handling (AC: 9)

  - [ ] Add try-catch blocks in all generation functions
  - [ ] Create fallback placeholder component for missing templates
  - [ ] Show specific error messages using toast.error()
  - [ ] Handle ZIP creation failures gracefully
  - [ ] Implement dependency resolution fallbacks
  - [ ] Log errors to console for debugging
  - [ ] Add error recovery tests

- [ ] Task 10: Add integration tests (AC: All)
  - [ ] Create `tests/integration/react-export.test.ts`
  - [ ] Test complete export cycle with sample blocks
  - [ ] Verify ZIP file structure and contents
  - [ ] Test error scenarios and fallbacks
  - [ ] Validate generated code syntax
  - [ ] Test with various block configurations

## Dev Notes

### Previous Story Context

**Story 1.12 Implementation** [Source: docs/stories/1.12.story.md]:

- Successfully implemented JSON export/import with dropdown menu infrastructure
- **ExportButton Component** (`components/layout/ExportButton.tsx`): Already has dropdown menu using shadcn/ui DropdownMenu
- **Export Utilities** (`lib/project/export.ts`): Contains `exportProject()`, `generateExportFilename()`, and `downloadJSON()` functions
- **Toast Notifications**: Sonner already integrated with `toast.success()` and `toast.error()` patterns
- **Testing Pattern**: Comprehensive test coverage with 387 tests, including integration tests
- **Key Learning**: Proper separation of concerns between UI and data layers

### Existing Components and Methods to Reference

**Block Data Structure** [Source: types/block.ts, Current Implementation]:

```typescript
interface Block {
  id: string // Unique identifier
  typeId: string // Template reference (e.g., "hero1", "navbar1")
  props: any // Template-specific customized content
  x: number // World coordinate position
  y: number
  width: number // Pixel dimensions
  height: number
  z: number // Layer order for stacking
  selected: boolean // UI selection state
}
```

**ExportButton Dropdown Pattern** [Source: components/layout/ExportButton.tsx]:

- Uses `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem` from `@/components/ui/dropdown-menu`
- Existing handler: `handleExportJSON()` - follow similar pattern for `handleExportReact()`
- Button styling: `variant="outline" size="sm" className="gap-2"`
- Icon usage: `Download` from lucide-react

**BlocksSlice Methods** [Source: store/slices/blocks.ts]:

- `useStore().blocks` - Array of all blocks in the canvas
- Blocks stored with complete state including positions and props

**Template Registry** [Source: lib/blocks/registry.ts]:

- `blockRegistry.getTemplate(typeId)` - Get template by typeId
- Templates have `component`, `defaultProps`, and `dependencies` properties
- Manual registration of all templates at initialization

**Canvas Helper Functions** [Source: components/canvas/Canvas.tsx]:

- `calculateCanvasDimensions()` - Returns canvas width and height based on blocks

**File Download Pattern** [Source: lib/project/export.ts]:

```typescript
export const downloadJSON = (data: any, filename: string): void => {
  const json = JSON.stringify(data, null, 2)
  const blob = new Blob([json], { type: 'application/json' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  a.click()
  URL.revokeObjectURL(url)
}
```

### File Locations Based on Project Structure

**New Files to Create** [Source: architecture/unified-project-structure.md]:

- `lib/blocks/template-sources.ts` - Template source code mapping
- `lib/project/globals.css.source.ts` - Static CSS source
- `lib/project/static-sources.ts` - Other static assets
- `lib/project/react-export.ts` - Main React export logic
- `lib/project/dependency-resolver.ts` - Dependency resolution
- Test files in corresponding `tests/unit/` directories

**Files to Modify**:

- `components/layout/ExportButton.tsx` - Add React export option
- `tests/unit/components/layout/ExportButton.test.tsx` - Update tests

### Template System Details

**Available Templates** [Source: lib/blocks/registry.ts, templates/ directory]:

- Hero templates: hero1, hero2, hero3, etc.
- Navigation: navbar1, navbar2, etc.
- Features: feature1, feature2, feature43, etc.
- About sections: about1, about2, about3, etc.
- Blog sections: blog1, blog7, etc.
- Footer templates: footer1, footer2, etc.

**Template Structure Pattern** [Source: Template analysis]:

- All templates are TypeScript React functional components
- Use TypeScript interfaces for props
- Import shadcn/ui components: `import { Button } from "@/components/ui/button"`
- Import icons: `import { IconName } from "lucide-react"`
- Export pattern: `export { ComponentName }`

### Dependencies for Generated React Project

**Core Dependencies** [Source: architecture/tech-stack.md, Template analysis]:

```json
{
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "lucide-react": "latest",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

### Testing Requirements

**Test Framework** [Source: architecture/testing-strategy.md]:

- Use Vitest with React Testing Library
- Mock file operations with `vi.fn()`
- Test file locations follow structure: `tests/unit/lib/project/[filename].test.ts`

**Required Test Coverage**:

1. Template source mapping and retrieval
2. Dependency resolution from templates
3. React component generation
4. ZIP file creation and structure
5. Export button interaction
6. Error handling scenarios
7. Integration test for complete export flow

### Implementation Strategy

1. **Build on Story 1.12**: Extend existing export dropdown infrastructure
2. **Template Processing**: Use existing template files as source, pre-process imports
3. **Maintain Simplicity**: Generate minimal viable React project
4. **Progressive Enhancement**: Start basic, add features incrementally
5. **Error Resilience**: Graceful degradation for missing components

### Critical Implementation Notes

1. **Import Path Translation**: Must convert `@/components/ui/*` to `./ui/*` in exported code
2. **Template Source Storage**: Store as TypeScript strings to preserve formatting
3. **Dependency Collection**: Parse imports from all used templates
4. **Position Preservation**: Use absolute positioning to maintain exact layout
5. **ZIP Generation**: Use JSZip library (needs installation: `npm install jszip @types/jszip`)
6. **Loading States**: Disable button and show loading during generation
7. **Error Messages**: Use specific, actionable error messages via toast

## Change Log

| Date       | Version | Description            | Author             |
| ---------- | ------- | ---------------------- | ------------------ |
| 2025-01-28 | 1.0     | Initial story creation | Bob (Scrum Master) |
| 2025-01-28 | 1.1     | Task 1 completed with refactored architecture | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-1-20250805

### Debug Log References

- Template source mapping system created successfully
- All 6 templates (hero1, navbar1, footer2, feature43, blog7, about3) extracted and stored
- Import paths converted from @/components/ui/* to ./ui/*
- shadcnblocks imports converted to relative paths
- Comprehensive test suite created with 20 tests, all passing
- Refactored to store each template source in individual files for better maintainability

### Completion Notes List

- Task 1 completed successfully
- Created template source mapping system with all 6 existing templates
- Implemented pre-processing of import paths for export compatibility
- Added utility functions: getTemplateSource(), getAvailableTemplates(), hasTemplateSource()
- Refactored architecture: Each template source now stored in individual .source.ts files
- Clean separation of concerns with template-sources.ts importing from dedicated source files
- All tests passing (20 tests total)

### File List

**Created:**
- lib/blocks/template-sources.ts - Template source code mapping system that imports from individual source files
- lib/blocks/template-sources/ - Directory for individual template source files
- lib/blocks/template-sources/hero1.source.ts - Hero1 template source with converted imports
- lib/blocks/template-sources/navbar1.source.ts - Navbar1 template source with converted imports
- lib/blocks/template-sources/footer2.source.ts - Footer2 template source with converted imports
- lib/blocks/template-sources/feature43.source.ts - Feature43 template source with converted imports
- lib/blocks/template-sources/blog7.source.ts - Blog7 template source with converted imports
- lib/blocks/template-sources/about3.source.ts - About3 template source with converted imports
- tests/unit/lib/blocks/template-sources.test.ts - Comprehensive test suite for template sources

**Modified:**
- None

## QA Results

_To be filled by QA Agent_
