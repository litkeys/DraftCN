# Story 1.12: Project Import and Export Functionality

## Status

Approved

## Story

**As a** user,
**I want** to export my project as a JSON file and import previously exported projects,
**so that** I can save my work externally and share or restore projects across sessions.

## Acceptance Criteria

1. Export button positioned on the right side of the top navigation bar
2. Import button positioned immediately to the left of the Export button
3. Both buttons styled consistently with existing navigation bar elements
4. Export button click triggers a dropdown/modal with export options:
   - Currently shows single option: "Export as JSON"
   - Dropdown designed to accommodate future export formats
5. Selecting "Export as JSON" generates a downloadable JSON file containing:
   - All block instances with their complete state (id, typeId, props, x, y, width, height, z, selected)
   - Canvas dimensions and configuration
   - Project metadata (export timestamp)
6. JSON file downloads with descriptive filename: `draftcn-project-[YYYY-MM-DD-HHmmss].json`
7. Import button click opens file picker dialog restricted to .json files
8. Upon selecting a valid JSON file:
   - File contents validated for correct structure and required fields
   - Confirmation dialog shown: "This will replace your current project. Continue?"
   - On confirmation, existing project state completely replaced with imported data
   - All blocks cleared and recreated from imported data
   - Canvas updates to reflect imported project state
9. Import validation handles:
   - Invalid JSON format with error message "Invalid file format"
   - Missing required fields with error message "Incomplete project data"
   - Corrupted data gracefully without crashing application
10. Import preserves all block properties including positions, dimensions, and z-ordering
11. After successful import, success notification shown: "Project imported successfully"
12. Export/Import operations do not affect zoom level or pan position (UI state remains separate from project data)

## Tasks / Subtasks

### Phase 1: Export Flow Implementation

- [x] Task 1: Create project export utility functions (AC: 5, 6, 12)

  - [x] Create `lib/project/export.ts` with export logic
  - [x] Implement `exportProject()` function to gather project data
  - [x] Include all blocks from BlocksSlice with complete state
  - [x] Add canvas dimensions from calculateCanvasDimensions
  - [x] Add project metadata (timestamp)
  - [x] Exclude UI state (zoom, pan) from exported data
  - [x] Create test file `tests/unit/lib/project/export.test.ts`

- [x] Task 2: Implement JSON download functionality (AC: 6)

  - [x] Create `downloadJSON()` utility function
  - [x] Generate filename with timestamp format
  - [x] Trigger browser download using Blob and anchor element
  - [x] Test file download in different browsers

- [x] Task 3: Create ExportButton component with dropdown (AC: 1, 3, 4)

  - [x] Create new component `components/layout/ExportButton.tsx`
  - [x] Install dropdown menu: `npx shadcn@latest add dropdown-menu`
  - [x] Create dropdown menu using shadcn/ui DropdownMenu component
  - [x] Add "Export as JSON" option as single menu item
  - [x] Style dropdown to match existing UI patterns
  - [x] Implement click handler for export option
  - [x] Style button consistently with existing Header elements
  - [x] Add test file `tests/unit/components/layout/ExportButton.test.tsx`

- [ ] Task 4: Update Header component with ExportButton (AC: 1)

  - [ ] Import ExportButton component
  - [ ] Position export button on right side of navigation bar
  - [ ] Ensure logo stays left, zoom control stays center, button on right
  - [ ] Update existing Header test to verify new button

### Phase 2: Import Flow Implementation

- [ ] Task 5: Create project import validation and parsing (AC: 8, 9)

  - [ ] Create `lib/project/import.ts` with import logic
  - [ ] Implement `validateProjectData()` function
  - [ ] Check for required fields: blocks array
  - [ ] Validate block structure (id, typeId, x, y, width, height, z)
  - [ ] Return validation errors for missing/invalid data
  - [ ] Create test file `tests/unit/lib/project/import.test.ts`

- [ ] Task 6: Create ImportButton component with file dialog (AC: 2, 3, 7)

  - [ ] Create new component `components/layout/ImportButton.tsx`
  - [ ] Create hidden file input element with accept=".json"
  - [ ] Trigger file picker on Import button click
  - [ ] Handle file selection event
  - [ ] Read file contents using FileReader API
  - [ ] Style button consistently with existing Header elements
  - [ ] Add test file `tests/unit/components/layout/ImportButton.test.tsx`

- [ ] Task 7: Implement confirmation dialog (AC: 8)

  - [ ] Install alert dialogue: `npx shadcn@latest add alert-dialog`
  - [ ] Use shadcn/ui AlertDialog component
  - [ ] Show warning message about replacing current project
  - [ ] Provide Cancel and Continue buttons
  - [ ] Handle user choice (proceed or cancel)

- [ ] Task 8: Implement project state replacement (AC: 8, 10)

  - [ ] On confirmation, call `clearBlocks()` from BlocksSlice
  - [ ] Iterate through imported blocks and call `addBlock()`
  - [ ] Ensure all block properties preserved (positions, dimensions, z-index)
  - [ ] Update canvas to reflect new state

- [ ] Task 9: Update Header component with ImportButton (AC: 2)

  - [ ] Import ImportButton component
  - [ ] Position import button immediately left of ExportButton
  - [ ] Ensure proper spacing and alignment
  - [ ] Update existing Header test to verify both buttons

- [ ] Task 10: Add error handling and notifications (AC: 9, 11)

  - [ ] Install Sonner: `npx shadcn@latest add sonner`
  - [ ] Setup Sonner in root layout with `<Toaster />` component
  - [ ] Import `toast` from "sonner" in relevant components
  - [ ] Show error toast for invalid JSON format: `toast.error("Invalid file format")`
  - [ ] Show error toast for incomplete project data: `toast.error("Incomplete project data")`
  - [ ] Show success toast after successful import: `toast.success("Project imported successfully")`

### Phase 3: Integration & Validation

- [ ] Task 11: Write end-to-end integration tests

  - [ ] Test complete export/import cycle
  - [ ] Test that UI state (zoom/pan) not affected by import/export
  - [ ] Test export data structure and content
  - [ ] Test import validation with various invalid inputs
  - [ ] Test successful import flow with valid data
  - [ ] Test toast notifications appear for success and error cases
  - [ ] Test Sonner setup in root layout

## Dev Notes

### Previous Story Context

Story 1.11 successfully implemented the zoom control system with:

- Virtual camera system using world/screen coordinate transforms
- Zoom state managed in UISlice (not persisted with document)
- All block data stored in world coordinates
- Comprehensive test coverage (302 tests passing)
- **Key Learning**: UI state (zoom, pan) must remain separate from document data

### Current Implementation State

**Existing Components** [Source: Current codebase analysis]:

1. **Header Component** (`components/layout/Header.tsx`):

   - Currently has logo on left, ZoomControl in center
   - Uses flexbox layout with relative positioning
   - Right side currently empty (has `<div className="flex-1" />`)
   - Ready for Import/Export buttons on right

2. **BlocksSlice** (`store/slices/blocks.ts`):

   - Manages all block state with `blocks` array
   - Has `clearBlocks()` action for clearing all blocks
   - Has `addBlock()` action for adding new blocks
   - Block interface includes all required fields for export

3. **UISlice** (`store/slices/ui.ts`):

   - Contains zoom and pan state (should NOT be exported)
   - These UI states are intentionally separate from document data

4. **Block Interface** (`types/block.ts`):

   ```typescript
   export interface Block {
     id: string
     typeId: string
     props: any // Template-specific props
     x: number
     y: number
     width: number
     height: number
     z: number
     selected: boolean
   }
   ```

5. **Canvas Component** (`components/canvas/Canvas.tsx`):
   - Has `calculateCanvasDimensions()` function for canvas size
   - Renders blocks from BlocksSlice
   - Will automatically update when blocks are replaced

### Import/Export Design

**Export Data Structure** [Source: Story requirements]:

```typescript
interface ProjectData {
  timestamp: string // ISO 8601 format
  canvas: {
    width: number // From calculateCanvasDimensions
    height: number
  }
  blocks: Block[] // All blocks with complete state
}
```

**File Naming Convention**:

- Format: `draftcn-project-[YYYY-MM-DD-HHmmss].json`
- Example: `draftcn-project-2025-01-25-143022.json`

### File Locations

**Based on Project Structure** [Source: architecture/unified-project-structure.md]:

- ExportButton: `components/layout/ExportButton.tsx`
- ImportButton: `components/layout/ImportButton.tsx`
- Export logic: `lib/project/export.ts`
- Import logic: `lib/project/import.ts`
- Tests:
  - `tests/unit/components/layout/ExportButton.test.tsx`
  - `tests/unit/components/layout/ImportButton.test.tsx`
  - `tests/unit/lib/project/export.test.ts`
  - `tests/unit/lib/project/import.test.ts`

### shadcn/ui Components to Use

**For Export Dropdown** [Source: architecture/tech-stack.md - shadcn/ui]:

- Use `DropdownMenu` component from `components/ui/dropdown-menu.tsx`
- Components: DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem

**For Import Confirmation** [Source: architecture/tech-stack.md - shadcn/ui]:

- Use `AlertDialog` component from `components/ui/alert-dialog.tsx`
- Components: AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogFooter, AlertDialogTitle, AlertDialogDescription, AlertDialogAction, AlertDialogCancel

**For Buttons** [Source: architecture/tech-stack.md - shadcn/ui]:

- Use existing `Button` component from `components/ui/button.tsx`
- Apply consistent variant and size with other Header buttons

**For Toast Notifications** [Source: shadcn/ui recommended]:

- Use **Sonner** component (modern toast solution)
- Install: `npx shadcn@latest add sonner`
- Add `<Toaster />` to root layout for global toast container
- Use `toast.error()` and `toast.success()` for notifications

### Implementation Approach

1. **Export Flow**:

   - User clicks Export button → Dropdown appears
   - User clicks "Export as JSON" → Gather data from store
   - Create JSON with blocks, canvas info, metadata
   - Trigger download with timestamped filename

2. **Import Flow**:

   - User clicks Import button → File picker opens
   - User selects .json file → Read and validate
   - Show confirmation dialog → User confirms
   - Clear existing blocks → Add imported blocks
   - Show success notification

3. **Validation Strategy**:
   - Use TypeScript type guards for structure validation
   - Check for required fields before processing
   - Gracefully handle errors without crashing
   - Provide clear error messages to user

### Testing Requirements

**Test Standards** [Source: architecture/testing-strategy.md]:

- Framework: Vitest with React Testing Library
- Mock file operations using vi.fn()
- Test file locations as specified above

**Required Test Cases**:

```typescript
describe('ExportButton', () => {
  it('should render export button in header')
  it('should show dropdown on click')
  it('should trigger export when option selected')
  it('should generate correct filename with timestamp')
})

describe('ImportButton', () => {
  it('should render import button in header')
  it('should open file picker on click')
  it('should validate imported JSON structure')
  it('should show confirmation dialog')
  it('should replace project state on confirm')
})

describe('Project Export', () => {
  it('should include all blocks with complete state')
  it('should include canvas dimensions')
  it('should include project metadata')
  it('should exclude UI state (zoom, pan)')
})

describe('Project Import', () => {
  it('should validate required fields')
  it('should handle invalid JSON gracefully')
  it('should preserve all block properties')
  it('should clear existing blocks before import')
})
```

### Technical Standards

**Technology Stack** [Source: architecture/tech-stack.md]:

- React 19.0+ with functional components
- TypeScript 5.3+ with strict mode
- Tailwind CSS 3.4+ for styling
- shadcn/ui for UI components
- Zustand 4.5+ for state management
- Vitest 1.0+ for testing

**Coding Standards** [Source: architecture/coding-standards.md]:

- Functional components with hooks only
- PascalCase for components (ExportButton, ImportButton)
- camelCase for functions (exportProject, validateProjectData)
- Never mutate state directly - use Zustand actions
- All interactive elements must have keyboard support

### Important Implementation Notes

1. **State Separation**: UI state (zoom, pan) must NEVER be included in export
2. **File Operations**: Use browser APIs (Blob, FileReader) for file handling
3. **Error Handling**: All file operations must have try-catch blocks
4. **Accessibility**: Buttons must be keyboard accessible
5. **Browser Compatibility**: Test file download/upload in Chrome, Firefox, Safari
6. **Performance**: Large projects with many blocks should export/import quickly
7. **Security**: Validate all imported data to prevent code injection
8. **Toast Notifications**: Use Sonner for all user feedback (success/error messages)

## Change Log

| Date       | Version | Description            | Author             |
| ---------- | ------- | ---------------------- | ------------------ |
| 2025-01-25 | 1.0     | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.1 (claude-opus-4-1-20250805)

### Debug Log References

N/A - No debugging issues encountered

### Completion Notes List

- Task 1 completed successfully
- Created exportProject() function to gather project data with blocks and canvas dimensions
- Implemented generateExportFilename() with timestamp format YYYY-MM-DD-HHmmss
- Created downloadJSON() utility for browser file download
- All 14 unit tests passing (100% coverage for export functionality)
- Properly excluded UI state (zoom, pan) from export data
- Task 2 completed successfully
- JSON download functionality already implemented in Task 1
- Created test page for browser compatibility testing at `/test-export`
- Download functionality tested and working correctly
- Task 3 completed successfully
- Created ExportButton component with shadcn/ui dropdown menu
- Installed and configured dropdown-menu component
- Styled button with Download icon consistent with Header design
- Dropdown shows "Export as JSON" option
- Click handler properly exports project data
- All 8 unit tests passing for ExportButton
- Ready for Task 4: Update Header component

### File List

**Created:**
- `lib/project/export.ts` - Export utilities (exportProject, generateExportFilename, downloadJSON)
- `tests/unit/lib/project/export.test.ts` - Comprehensive test suite for export functions
- `app/test-export/page.tsx` - Test page for browser download functionality
- `components/layout/ExportButton.tsx` - Export button with dropdown menu
- `components/ui/dropdown-menu.tsx` - shadcn/ui dropdown menu component
- `tests/unit/components/layout/ExportButton.test.tsx` - ExportButton component tests

**Modified:**
- None

## QA Results

[To be filled by QA Agent]
